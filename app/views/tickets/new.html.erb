<!-- このページにパンくずリストを作るとなぜかエラーになる -->
<% provide(:title, t("ticket.title.new")) %>
<% provide(:button_text, t("helpers.submit.create")) %>
<% provide(:link_to, project_tickets_path(@project)) %>
<%= render 'form'%>