<%= form_for(@comment, url: yield(:link_to)) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= f.label :content %>
  <%= f.text_area :content, class: 'form-control input-lg' %>

  <%= f.submit yield(:button_text), class: "btn btn-primary" %>
<% end %>
